from django import template


#register = template.Library()
#@register.filter(name='cut')
#def cut(value,arg):
    '''
    Cuts out alll val of arg from string
    '''
    #return.replace(arg,'')

#register.filter('cut',cut)

